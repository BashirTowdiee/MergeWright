# OpenCode CLI Backend

`opencode-cli` is supported as a configured backend type and can be probed with `probe-opencode`.

Current support includes contract probing and read-only dry-run routing validation for selected roles. Real write-role execution remains restricted.
