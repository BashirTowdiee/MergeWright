# Provider Utility Commands

## probe-opencode

```bash
npm run agent -- probe-opencode [--config <config-path>] [--backend <name>] [--command <command>] [--json] [--validate-readonly-contract]
```

Notes:

- contract probe only (`version/help/run --help`)
- does not execute agent prompts
- does not write output files
