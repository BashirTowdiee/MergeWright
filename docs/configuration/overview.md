# Configuration Overview

Project configuration defines workspace paths, prompts/stages/runs paths, safety gates, checks, and backend/agent routing.

Pass `--config <config-path>` for config-bound commands.
