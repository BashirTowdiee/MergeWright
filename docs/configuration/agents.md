# Agents

Agent role mapping:

- planner phases use `agents.planner`
- builder and fix execution use `agents.builder`
- reviewer phases use `agents.reviewer`

Role config includes backend name, model, and reasoning effort.
