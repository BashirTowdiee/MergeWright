# Change Report Policy

Run reports are generated by `report-run` and optional `--generate-report` on `run`/`continue-run`.

When configured, policy controls output expectations while command flags still determine output mode (`--json`, `--stdout-only`, `--pr-summary`).
