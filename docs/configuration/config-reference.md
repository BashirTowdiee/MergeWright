# Config Reference

Key areas:

- `workspaceRoot`, `stagesDir`, `promptsDir`, `runsDir`
- `commands.checks`
- `writeSafety`
- `executionBackends`
- `agents`
- legacy `codex` role config (compatibility)
