# Provider Switching Example

Use backend mappings in config:

- define `executionBackends`
- assign `agents.planner/backend`, `agents.builder/backend`, `agents.reviewer/backend`
- validate OpenCode contract with `probe-opencode`
