# Backend Architecture

Execution backend adapters translate role execution requests into provider-specific command invocations while preserving orchestrator safety and artefact contracts.

See also: [../configuration/execution-backends.md](../configuration/execution-backends.md).
