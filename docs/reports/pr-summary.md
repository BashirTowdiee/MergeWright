# PR Summary

`--pr-summary` generates `pr-summary.md` from the run change report.

```bash
npm run agent -- report-run <run-id> --config <config-path> --pr-summary
```

`--pr-summary --stdout-only` prints PR summary markdown only.
