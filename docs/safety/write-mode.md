# Write Mode

Write mode is explicit via `--allow-writes`.

- default execution is read-only
- write mode applies to builder/fix-style execution only
- planner/reviewer/reassessor remain read-only/classification-only
