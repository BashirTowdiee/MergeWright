# GitHub repository configuration

This directory contains repository-level GitHub configuration for MergeWright.

Current contents may include:

- pull request templates
- issue templates
- repository ownership metadata
- workflow configuration

Keep these files small and focused. User-facing product documentation belongs under `docs/`.
