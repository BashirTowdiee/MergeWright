# VS Code workspace settings

This directory contains optional editor configuration for contributors using VS Code.

The settings are intentionally minimal:

- Prettier is recommended as the default formatter.
- Format-on-save is disabled so formatting remains an explicit developer action.

Project behaviour should not depend on VS Code-specific configuration.
